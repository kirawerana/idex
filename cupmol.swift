// Create an instance of ContentShapeKinds with multiple options
let options: ContentShapeKinds = [.option1, .option2]

// Check if an option is contained in the set
if options.contains(.option1) {
    print("Option 1 is selected")
}
